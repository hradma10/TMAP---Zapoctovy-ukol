<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleCalculator"
        tools:targetApi="31">
        <activity
            android:name="cz.tmap.converterApp.fragmentsAndActivities.MainActivity"
            android:launchMode="singleTask"
            android:exported="true"
            android:theme="@style/NotTopNameStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="cz.tmap.converterApp.fragmentsAndActivities.ConvActivity"
            android:exported="true"
            android:theme="@style/NotTopNameStyle">
        </activity>
    </application>
</manifest>